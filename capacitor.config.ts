import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'be.chrverviers.stockmanager',
  appName: 'StockManager',
  webDir: 'dist/stockmanager',
  bundledWebRuntime: false
};

export default config;
